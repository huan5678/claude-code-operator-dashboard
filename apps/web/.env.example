# 通常不需要這個檔案。
# Vite 已設定為從 monorepo root 的 .env 讀 GOOGLE_CLIENT_ID
# 並自動注入為 import.meta.env.VITE_GOOGLE_CLIENT_ID。
#
# 只有在你想覆蓋 root 設定（例如 dev 跟 prod 用不同 OAuth Client）時才需要：
# VITE_GOOGLE_CLIENT_ID=xxxxxxxxxxxx.apps.googleusercontent.com
